# CLAUDE.md

このファイルは、Claude Code (claude.ai/code) がこのリポジトリで作業する際のガイダンスを提供します。

## コマンド

### 開発
```bash
pnpm dev        # 開発サーバーを起動 (http://localhost:3000)
pnpm build      # 本番用にビルド
pnpm start      # 本番サーバーを起動
pnpm lint       # ESLintを実行
```

### パッケージ管理
このプロジェクトは **pnpm** をパッケージマネージャーとして使用しています。npmではなく常にpnpmコマンドを使用してください：
```bash
pnpm install    # 依存関係をインストール
pnpm add <pkg>  # 新しい依存関係を追加
```

## アーキテクチャの概要

### プロジェクト構造
- **Next.js 16** App Router使用（Pages Routerではない）
- **TypeScript** strict modeが有効
- **Tailwind CSS v4** スタイリング用
- パスエイリアス: `@/*` は `./src/*` にマップ

### メインアプリケーション
中国語の声調（四声）クイズゲームが `src/app/game.tsx` にあります。ユーザーが中国語の文字の声調を当てる単一ページのインタラクティブなクイズです。

主要コンポーネント：
- `src/app/page.tsx` - ToneQuizコンポーネントをレンダリングするホームページ
- `src/app/game.tsx` - すべてのゲームロジックを含むメインクイズコンポーネント
- `src/app/layout.tsx` - Geistフォントを使用するルートレイアウト

### スタイリング方針
以下の組み合わせを使用：
1. Tailwind CSS - ユーティリティクラス用
2. インラインCSS-in-JS - コンポーネント固有のスタイル用
3. keyframesで定義されたCSSアニメーション

UIデザインは落ち着いた中華カラーを基調としており、以下の色合いを使用：
- 背景：アンバー、オレンジ、レッドのグラデーション
- アクセント：ゴールド/イエロー
- ボタン・カード：レッド系、グレー系、各HSKレベルに応じた色分け

### 状態管理
現在はReactの組み込みuseStateフックを使用。外部の状態管理ライブラリは使用していません。

### データ構造
クイズデータは現在game.tsxコンポーネント内に `quizData` 配列としてハードコードされています。各クイズアイテムには以下が含まれます：
- `character`: 中国語の文字
- `correctTone`: 声調番号の配列（1-4）
- `difficulty`: easy/medium/hard

## 重要な注意点

- テスティングフレームワークは現在設定されていません
- ゲームは声調マークのレンダリングにSVGコンポーネントを使用
- 視覚的フィードバックのために紙吹雪とシェイクアニメーションを実装
- プロジェクトは初期開発段階で、初期セットアップ後のコミットは1つのみ